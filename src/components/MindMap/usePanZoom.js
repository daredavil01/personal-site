// Promoted to src/hooks/usePanZoom.js for the atlas map hub (§4.6); this
// shim keeps the old import path working until MindMap migrates.
import usePanZoom from '../../hooks/usePanZoom';

export default usePanZoom;
